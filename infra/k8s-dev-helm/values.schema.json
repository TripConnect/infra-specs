{
  "$schema": "https://json-schema.org/draft-07/schema",
  "type": "object",
  "properties": {
    "deploymentType": {
      "type": "string",
      "description": "Controls which resource group this release renders.",
      "enum": ["backend-service", "infra"],
      "default": "backend-service"
    },
    "runtime": {
      "type": "string",
      "description": "Application runtime. Use nodejs to inject Node.js OpenTelemetry auto-instrumentation env vars.",
      "enum": ["", "nodejs"],
      "default": ""
    }
  }
}
